C언어에서는 두 함수의 이름이 같고 매개변수의 type이 다른 경우를 허용하지 않으나 C++에서는 가능하다.
함수 호출 시 전달되는 인자를 통해서 호출하고자 하는 함수의 구분이 가능하다.
이러한 형태의 함수정의를 '함수 오버로딩'이라고 한다.

왜 C에서는 안될까?
=> C에서는 함수 호출 시 이름을 통해 search하지만 C++은 이름과 매개변수를 모두 활용한다.