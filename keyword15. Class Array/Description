 1. 객체 배열
    구조체 배열처럼 객체 배열 선언이 가능하며 동적할당도 가능하다.
    배열 선언시에도 생성자는 호출 된다.
    ※ 단, 배열의 경우 생성자를 별도로 명시하지 못한다. 즉 생성자에 인자를 전달 할 수 없다.
       따라서 class_name() { ... } 형태의 생성자가 반드시 존재 해야 한다.
    또, 배열 선언 후 변수 초기화를 원한다면 일일히 초기화 하는 과정이 필요하다.