 1. 멤버함수 동작 원리
    객체 생성 시 멤버변수는 객체 내에 존재하지만 멤버함수는 메모리의 한 공간에 별도로 위치하며 
    이 함수가 정의된 클래스의 모든 객체가 이를 공유하는 형태를 취한다.

 2. 가상함수 동작 원리
    하나 이상의 가상함수를 포함하는 클래스에 대해서는 컴파일러가 '가상함수 테이블'이라는 것을 만든다.
    V-Table (Virtual Table)이라고도 하는데, 이는 실제 호출되어야 할 함수의 위치정보를 담고 있는 테이블이다.
    key는 호출하고자 하는 함수 구분자, value는 함수의 주소정보를 알려주는 역할을 한다.
    오버라이딩 된 가상함수의 주소 정보는 sub class의 가상함수 테이블에 포함되지 않기 때문에, 오버라이딩된 가상함수를 호출 하면
    무조건 마지막에 오버라이딩을 한 유도 클래스의 멤버함수가 호출된다.

    가상함수 테이블은 객체의 생성과 상관없이 메모리 공간에 할당 된다.

    ==> 가상함수가 클래스에 포함될 경우, 가상함수 테이블이 생성되고 또 이 테이블을 참조하여 호출될 함수가 결정되기 때문에 실행속도가 감소한다. (미미한 수준)